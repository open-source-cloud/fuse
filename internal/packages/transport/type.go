package transport

import "github.com/open-source-cloud/fuse/pkg/transport"

// Internal communication transport type (separate from other types as only internally supported)
const Internal transport.Type = "internal"
