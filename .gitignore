# Binary files
bin/

# Test reports
*.xml

# IDE specific files
.idea/
.vscode/
*.swp
*.swo

# OS specific files
.DS_Store
Thumbs.db

# All envs are not allowed (only .env.example)
.env
.env.*
!.env.example

# Swagger generated files
docs/docs.go
docs/swagger.json
docs/swagger.yaml

.cursor/hooks/state

# Do not commit data/fuse directory because is the database object store for the engine 
# Its used to save all the schema, package, workflow definition schemas and state data
data/fuse